# macOS
.DS_Store

# Claude Code personal memory — never public
.claude/

# VS Code workspace file
*.code-workspace

# npm (used for build scripts only — not part of the extension)
package.json
package-lock.json

# Packaged builds
*.zip
*.crx
